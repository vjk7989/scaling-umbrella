import React from "react";

const Title = ({ title }) => {
  return (
    <div className="heading-section">
      <h2 className="tf-title pb-30">{title}</h2>
    </div>
  );
};

export default Title;
